# Leave unset for local dev — falls back to http://localhost:3006(see src/config/site.js).
# Uncomment and set once a real MA Universal backend domain exists.
# VITE_API_URL=https://backend.mauniversalinc.com
VITE_API_URL=http://localhost:3006
